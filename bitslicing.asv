clc;
clear all;
close all;

%% Read image
original_image = imread('Image/Birds.jpg');
figure(1)
imshow(original_image)
title('Original Image')

%% 8-bit plane slicing of the image
img = double(imresize(original_image,[10,10]));
bits = 8;
for i = 1 : bits
  b = mod(img, 2);
  img = floor(img/2); 
  figure(i)
  imshow(a)
  title(sprintf('Bit Plane: %d', i+1))
end

